package Week_one.Assignment_Three.collection;

import java.awt.*;
import java.util.HashMap;

public class DemoHashMap {
    public static void main(String[] args) {
        HashMap<String, Point> points = new HashMap<>();

        //put the key value pair in hashmap
        points.put("x1", new Point(0,0));
        points.put("x2", new Point(0, 5));
        points.put("x2", new Point(5, 5));
        points.put("x2", new Point(0, 0));

        // retrieve a value for a key using the get method

    }
}
